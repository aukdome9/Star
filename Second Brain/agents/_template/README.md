# Agent template

Copy this entire `_template` folder to `agents/<your-agent-id>/` and rename.

1. Fill `README.md` with purpose + status  
2. Drop domain material into `content/`  
3. Define rhythm in `cadence/`  
4. Put markdown skills in `skills/`  
5. Log durable lessons in `memory/`  
